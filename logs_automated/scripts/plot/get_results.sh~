#!/bin/bash
FILES=../../server/*
for f in $FILES
do
	filename=$(basename $f)
	cat $f | grep sec | head -$(cat $f | wc -l) | tr - " " | awk '{print $4,$8}' > probs/$filename
	
	#echo "This is file $f"
done
